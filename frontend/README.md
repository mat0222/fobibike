# Frontend FobiBike

App React del panel de administración. Ver [README principal](../README.md) para instalación y configuración.

```powershell
npm install
npm run dev
```

Rutas: `/login`, `/` (inventario), `/ingresos`, `/fobi`.
