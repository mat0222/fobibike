# Legacy — FobiBike v1 (PHP + XAMPP)

Sistema anterior archivado. **No desplegar ni exponer públicamente.**

| Archivo | Descripción |
|---------|-------------|
| `config.php` | Conexión MySQL (credenciales XAMPP) |
| `login.*` | Login estático + sesión PHP |
| `index.html`, `inicio.*` | Dashboard admin monolítico |
| `*_productos.php` | CRUD sin autenticación |

**Stack activo:** `../backend/` + `../frontend/`
